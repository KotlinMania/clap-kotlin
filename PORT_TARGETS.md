# Port targets — `clap-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `clap` that codex-rs uses, and therefore the minimum subset that `clap-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-30] (rank `50`, tier `direct`, status `To Do`)

**Cargo package name**: `clap`
**Rust module name (in `use` statements)**: `clap`

## Summary

- **8** distinct symbols imported by codex-rs
- **50** total `use` statements
- **34** codex source files affected
- **21** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `clap::Parser`

- **29 `use` site(s)** across **20 codex member crate(s)**: `codex-app-server`, `codex-app-server-protocol`, `codex-app-server-test-client`, `codex-chatgpt`, `codex-cli`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-protocol/src/bin/export.rs:2` (`codex-app-server-protocol`) — `use clap::Parser`
- `app-server-protocol/src/bin/write_schema_fixtures.rs:3` (`codex-app-server-protocol`) — `use clap::Parser`
- `app-server-test-client/src/lib.rs:26` (`codex-app-server-test-client`) — `use clap::Parser`
- `app-server/src/main.rs:1` (`codex-app-server`) — `use clap::Parser`
- `chatgpt/src/apply_command.rs:2` (`codex-chatgpt`) — `use clap::Parser`
- `cli/src/app_cmd.rs:1` (`codex-cli`) — `use clap::Parser`
- `cli/src/lib.rs:4` (`codex-cli`) — `use clap::Parser`
- `cli/src/main.rs:3` (`codex-cli`) — `use clap::Parser`
- `cli/src/marketplace_cmd.rs:4` (`codex-cli`) — `use clap::Parser`
- `cloud-tasks/src/cli.rs:2` (`codex-cloud-tasks`) — `use clap::Parser`
- `core/src/bin/config_schema.rs:2` (`codex-core`) — `use clap::Parser`
- `debug-client/src/main.rs:16` (`codex-debug-client`) — `use clap::Parser`
- `exec/src/cli.rs:3` (`codex-exec`) — `use clap::Parser`
- `exec/src/main.rs:1` (`codex-exec`) — `use clap::Parser`
- `execpolicy-legacy/src/main.rs:2` (`codex-execpolicy-legacy`) — `use clap::Parser`
- `execpolicy/src/execpolicycheck.rs:6` (`codex-execpolicy`) — `use clap::Parser`
- `execpolicy/src/main.rs:2` (`codex-execpolicy`) — `use clap::Parser`
- `file-search/src/cli.rs:5` (`codex-file-search`) — `use clap::Parser`
- `file-search/src/main.rs:3` (`codex-file-search`) — `use clap::Parser`
- `linux-sandbox/src/linux_run_main.rs:1` (`codex-linux-sandbox`) — `use clap::Parser`
- _… and 9 more (see `codex_external_usage.json` for the full list)._

### `clap::Args`

- **6 `use` site(s)** across **6 codex member crate(s)**: `codex-app-server-transport`, `codex-cli`, `codex-cloud-tasks`, `codex-exec`, `codex-tui`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/auth.rs:5` (`codex-app-server-transport`) — `use clap::Args`
- `cli/src/main.rs:1` (`codex-cli`) — `use clap::Args`
- `cloud-tasks/src/cli.rs:1` (`codex-cloud-tasks`) — `use clap::Args`
- `exec/src/cli.rs:1` (`codex-exec`) — `use clap::Args`
- `tui/src/cli.rs:1` (`codex-tui`) — `use clap::Args`
- `utils/cli/src/shared_options.rs:4` (`codex-utils-cli`) — `use clap::Args`

### `clap::ValueEnum`

- **5 `use` site(s)** across **4 codex member crate(s)**: `codex-app-server-transport`, `codex-exec`, `codex-state`, `codex-utils-cli`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/auth.rs:6` (`codex-app-server-transport`) — `use clap::ValueEnum`
- `exec/src/cli.rs:4` (`codex-exec`) — `use clap::ValueEnum`
- `state/src/bin/logs_client.rs:7` (`codex-state`) — `use clap::ValueEnum`
- `utils/cli/src/approval_mode_cli_arg.rs:1` (`codex-utils-cli`) — `use clap::ValueEnum`
- `utils/cli/src/sandbox_mode_cli_arg.rs:1` (`codex-utils-cli`) — `use clap::ValueEnum`

### `clap::ArgAction`

- **4 `use` site(s)** across **4 codex member crate(s)**: `codex-app-server-test-client`, `codex-debug-client`, `codex-file-search`, `codex-utils-cli`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-test-client/src/lib.rs:25` (`codex-app-server-test-client`) — `use clap::ArgAction`
- `debug-client/src/main.rs:15` (`codex-debug-client`) — `use clap::ArgAction`
- `file-search/src/cli.rs:3` (`codex-file-search`) — `use clap::ArgAction`
- `utils/cli/src/config_override.rs:1` (`codex-utils-cli`) — `use clap::ArgAction`

### `clap::FromArgMatches`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-exec`, `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `exec/src/cli.rs:2` (`codex-exec`) — `use clap::FromArgMatches`
- `tui/src/cli.rs:2` (`codex-tui`) — `use clap::FromArgMatches`

### `clap::Subcommand`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-app-server-test-client`, `codex-execpolicy-legacy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-test-client/src/lib.rs:27` (`codex-app-server-test-client`) — `use clap::Subcommand`
- `execpolicy-legacy/src/main.rs:3` (`codex-execpolicy-legacy`) — `use clap::Subcommand`

### `clap::CommandFactory`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-cli`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cli/src/main.rs:2` (`codex-cli`) — `use clap::CommandFactory`

### `clap::ArgGroup`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-cli`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cli/src/mcp_cmd.rs:8` (`codex-cli`) — `use clap::ArgGroup`
